[project]
name = "bikelanes"
version = "0.1.0"
description = ""
authors = [
    {name = "system-errors",email = "systemerror@proton.me"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "streamlit (>=1.46.0,<2.0.0)",
    "geopandas (>=1.1.0,<2.0.0)",
    "folium (>=0.20.0,<0.21.0)",
    "streamlit-folium (>=0.25.0,<0.26.0)",
    "shapely (>=2.1.1,<3.0.0)"
]

[tool.poetry]
name = "bikelanes"
version = "0.1.0"
description = "A dashboard showing unsafe suggested bike lanes using open data"

[tool.poetry.group.dev.dependencies]
osmnx = "^2.0.4"
geopandas = "^1.1.0"
folium = "^0.20.0"
matplotlib = "^3.10.3"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
